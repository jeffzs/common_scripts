# get baseline system information

Sys.getenv("HOME") # check current HOME env
system("df -h") # check available disk space
system("free -m") # check available memory in MBs
system("ip addr show") # show ip addr
